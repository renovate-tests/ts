box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: lint
        code: eslint test/*.js
    - npm-test

  after-steps:
    - wantedly/pretty-slack-notify:
        webhook_url: $SLACK_WEBHOOK_URL
