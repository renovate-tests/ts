box: wercker/ubuntu12.04-nodejs0.10
build:
  steps:
    - npm-install

    - script:
      name: install bower
      code: sudo npm install -g bower

    - script:
      name: run bower install
      code: bower install

    - npm-test
