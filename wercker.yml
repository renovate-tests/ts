box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: lint
        code: $(npm bin)/eslint test/*.js
    - npm-test

  after-steps:
    - wantedly/pretty-slack-notify:
        webhook_url: $SLACK_WEBHOOK_URL
